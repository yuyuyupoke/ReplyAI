---
description: Deploy changes to GitHub
---

1. Run `git add .`
2. Run `git commit -m "<COMMIT_MESSAGE>"`
   - If the user provided a message, use it.
   - If not, infer a concise commit message based on the recent changes (e.g., "Feat: Add new feature" or "Fix: Resolve bug").
3. Run `git push origin main`
